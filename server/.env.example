# Railway Environment Variables for Ryazhenka Chat Server

# Server Port (Railway will set this automatically, but you can override)
PORT=3000

# Environment (development or production)
NODE_ENV=production

# Optional: Database configuration (if using MongoDB)
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/ryazhenka

# Optional: PostgreSQL configuration  
# DATABASE_URL=postgresql://user:password@localhost:5432/ryazhenka

# Optional: Redis cache
# REDIS_URL=redis://default:password@host:port

# Optional: API Keys
# API_SECRET=your_api_secret_here

# Optional: CORS settings
# ALLOWED_ORIGINS=https://example.com,https://another.com
